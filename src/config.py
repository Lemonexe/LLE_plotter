plot_colors = ['black', 'red', 'blue', 'green', 'purple', 'orange']
plot_linestyles = ['-', '--', ':', '-.']
# plot_markers = ['o', '^', 'v', 'x'] # not used, the markers are not easily distinguishable since they lie on eq. curve

max_colors = len(plot_colors)
max_linestyles = len(plot_linestyles)
